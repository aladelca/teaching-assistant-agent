# HTTP LLM provider
LLM_API_URL=https://api.example.com/v1/responses
LLM_API_KEY=replace_me
LLM_MODEL=gpt-4.1
LLM_PAYLOAD_MODE=messages
LLM_RESPONSE_PATH=choices.0.message.content

# OpenAI Agents SDK provider
OPENAI_API_KEY=replace_me

# Codex CLI provider (requires `codex login`)
CODEX_MODEL=gpt-5
CODEX_REASONING_EFFORT=high

# Optional local review UI token
REVIEW_UI_TOKEN=replace_me
